type Feedback @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  email: AWSEmail
  message: String
}

type Signup @model @auth(rules: [{allow: public}]) {
  id: ID!
  email: AWSEmail
}
 